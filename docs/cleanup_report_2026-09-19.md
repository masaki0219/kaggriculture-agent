# Cleanup report — 2026-09-19

This report records repository organization only. It does not revise research
strategy or propose new experiments.

## Baseline

- `git pull --ff-only`: already up to date.
- Initial HEAD: `7b744dd Add M-family experiments and evaluation artifacts`.
- Initial worktree differences: local-only
  `data/replays/2026-09-18/live_population_before_e21/` and pre-existing
  `.DS_Store` changes inside `public_agents/gzmcr`.
- SHA256 inventory: 3,537 files were hashed before cleanup, excluding `.git`
  and `.venv` but including local replay data and external submodule contents.
- No project-owned broken symlink was present.

## Before and after

Before, the relevant root structure included:

```text
.
├── README.md
├── analysis/ archive/ artifacts/ candidates/ data/ docs/
├── evaluation/ experiments/ public_agents/ tools/
├── submission_e22.tar.gz
└── submission_e22_single/
    └── main.py
```

After cleanup:

```text
.
├── README.md
├── .gitignore
├── .gitmodules
├── analysis/
├── archive/
│   ├── bundles/
│   └── setup/
├── artifacts/
│   ├── bundles/current/
│   └── submissions/
│       ├── e10_kaito27/
│       ├── e11_prvsiyan_frontier/
│       ├── e21_tetsu_market_v23/
│       ├── e22_market_policy_swap/
│       └── tetsu_demand_preserving/
├── candidates/2026-09-19_public/
├── data/{corpora,meta_cache,replays}/
├── docs/history/reorganization_2026-09-18/
├── evaluation/
├── experiments/
├── public_agents/
└── tools/
```

## Classification used

- Canonical: submitted exact bytes, current runtime bundle, public candidate
  source/submission pairs, active tools, evaluation output, and replay/corpus
  evidence.
- Historical: immutable numbered experiments, legacy series, superseded bundle
  contents, and the 2026-09-18 reorganization record.
- Disposable: generated bytecode, Finder metadata outside submodules, candidate
  download logs, the failed E22 multi-file package, and byte-identical copies
  with another retained historical canonical file.

## Moved or copied

- Moved `artifacts/bundles/current/submission_e11.tar.gz` to
  `artifacts/submissions/e11_prvsiyan_frontier/submission.tar.gz`. The runtime
  raw source archive remains in the bundle.
- Moved `artifacts/submissions/submission_kaito27_current.tar.gz` to
  `artifacts/submissions/e10_kaito27/submission.tar.gz`.
- Copied the exact E21 archive from the runtime raw source to
  `artifacts/submissions/e21_tetsu_market_v23/submission.tar.gz`.
- Moved the accepted E22 standalone file from `submission_e22_single/main.py`
  to `artifacts/submissions/e22_market_policy_swap/main.py`.
- Copied the submitted Tetsu Demand-Preserving archive to
  `artifacts/submissions/tetsu_demand_preserving/submission.tar.gz`; its source
  and acquisition archive remain together under `candidates/`.
- Moved the four 2026-09-18 reorganization/README-planning records to
  `docs/history/reorganization_2026-09-18/`.
- Moved the unique original E11 finalizer bytes from
  `archive/duplicates/finalize_e11_2.py` to
  `archive/setup/finalize_e11_original.py`.

Every canonical submission directory now contains `metadata.json` and
`sha256.txt`.

## Deleted

- `submission_e22.tar.gz`: failed Kaggle API 400 multi-file packaging output.
- Four candidate notebook download `*.log` files under
  `candidates/2026-09-19_public/`.
- Three byte-identical old installer copies under `archive/duplicates/`; the
  same historical bytes remain in
  `archive/setup/install_e11_challengers_original.py`.
- Seven project-owned `__pycache__/` directories.
- Nine project-owned `.DS_Store` files outside external submodules.
- Empty root `submission_e22_single/` and empty `archive/duplicates/`
  directories after their contents were handled.

No file under `data/replays/`, `data/corpora/`, or an external submodule was
deleted or edited.

## Preserved canonical artifacts

| Artifact | Status | SHA256 |
| --- | --- | --- |
| `artifacts/submissions/e11_prvsiyan_frontier/submission.tar.gz` | submitted | `f0e698738d95ad3822d3128c814aeb8548d8b58e2e156bd1a3865a376ccdc1ad` |
| `artifacts/submissions/e10_kaito27/submission.tar.gz` | submitted | `e9ce1efb00f8cce0d51f6b8b8a37d061f5e4ec3e6446fc2f45d6d2a0e5b92e25` |
| `artifacts/submissions/e21_tetsu_market_v23/submission.tar.gz` | submitted | `3f252be79997944c3b724e6577f1a68886a4a81377b4fbdeb0fdfa3db83081ef` |
| `artifacts/submissions/e22_market_policy_swap/main.py` | submitted | `72ba21f3ebb7e38df212589969dfad8ab8bf675d83387f715d03a8b9a9ee6e85` |
| `artifacts/submissions/tetsu_demand_preserving/submission.tar.gz` | submitted | `d2d4d0dacc170b1e9e2718a255f332902ba0e3c9c981e7190587bbd09a6bc285` |

The Tetsu Demand-Preserving, Alperen First-in-Line, Ahmed V47, and Sunil V8
exact source/submission pairs remain under `candidates/2026-09-19_public/`.
E30-E32 source, screens, and `results.md`/`results.json` remain unchanged.
E33 source, builder, screen, and README remain unchanged.

## Replay and corpus preservation

| Path | Size | Git state / note |
| --- | ---: | --- |
| `data/replays/2026-09-18/live_population/` | 2.5 GB | ignored local extraction; preserved |
| `data/replays/2026-09-18/live_population_before_e21/` | 2.5 GB | untracked local backup; preserved |
| `data/replays/2026-09-18/live_population.zip` | 50 MB | tracked; preserved |
| `data/replays/2026-09-18/live_population_before_e21.zip` | 50 MB | tracked; preserved |
| `data/corpora/2026-09-18/m_family.zip` | 60 MB | tracked; preserved |

The two replay ZIP files are byte-identical
(`96e365c5219cd57d5aed25bf330c45e217a1be935348b086976f7c0600b43eb2`),
and the extracted replay trees also compare byte-identically. Both were kept
because replay deletion was explicitly out of scope and the backup name records
local provenance.

`tools/acquisition/refresh_live_population.py` now derives its dated output
directory from the local execution date, preventing a later refresh from
overwriting the hard-coded `2026-09-18` location.

## Verification

- Parsed 400 project-owned Python files with `ast.parse`: passed.
- Imported E11, E21, and `elite_runtime` with bytecode writes disabled: passed.
- Parsed 76 project-owned JSON files: passed.
- Checked all active shell scripts with `bash -n`: passed.
- Verified all five canonical submission hashes from their `sha256.txt`: passed.
- Tested project-owned ZIP and tar/gzip archives: passed.
- Checked symlinks outside `.git` and `.venv`: no broken link.
- Checked `.gitmodules` paths and recorded SHAs: all seven paths remain present
  and the superproject gitlink SHAs are unchanged.
- Confirmed E22 standalone, Tetsu source/submission, Alperen
  source/submission, and E30-E33 directories remain present.
- Confirmed `README.md` has no diff.
- No arena or submission command was run.

## Unresolved items

- `public_agents/gzmcr` was already dirty because of `.DS_Store` files inside
  that external submodule. It was deliberately not modified.
- `data/replays/2026-09-18/live_population_before_e21/` remains intentionally
  untracked and local-only.
- E33 had no separate `results.md` or `results.json` at the initial audit; no
  result file was invented during cleanup.
- Strategy/state documentation, including stale statements in existing files,
  was not rewritten. This includes the root `README.md` by explicit request.
