# 次の担当モデルへの研究仕様 — 2026-09-19

この文書は次の担当（GPT-5.6-solを含む）が、方針をゼロから考え直さず着手するための決定記録。
新しいライブ証拠が反証する場合は理由を記録して更新してよい。モデル名は手法の正しさの根拠にしない。

## 1. 最初にすること

- 最新HEAD・working tree・READMEを確認する。
- `analysis/g6d01/pilot_summary.json` と `report.md` を読み、G6D01を本番候補と誤認しない。
- `python analysis/g6d01/prepare_workspace.py` で既存コードから再現用runtimeを用意する。元の提出・既存E番号のコードは書き換えない。
- 次の新しい方策をE34以降の未使用番号で作る。G6D01をE34へ改名するだけの作業はしない。
- 最初の実験テーマは **P1: 回収と肥料確保の連動**。P0の対戦相手拡充も同時に進める。

## 2. 何を信じてよいか

| 主張 | 証拠の強さ | 使い方 |
|---|---|---|
| 現行Tetsuで満杯の卵の未回収による損失がある | 3ライブ試合の公式再現、うち2敗の容量損失差8/12個 | P1の発火条件と説明対象 |
| 卵回収だけを増やせば強くなる | 支持不十分。肥料不足に伴うイチゴ減少、5比較で差額悪化 | 無条件回収を採用しない |
| G6D01はライブ勝率を上げる | 未確認。改善した勝敗はTetsu同型の引き分けのみ | 昇格根拠にしない |
| 終盤ニンジンが有望 | Ace戦1例で23個不足、小麦19個過多 | P2で店舗条件を分けて検証 |
| 売り時にも改善余地がある | 未来情報を使った限定探索で差額＋40〜978 | 診断。実装可能な性能・全体上限とは扱わない |
| 2600付近が理論上限 | 分からない。レーティングの推移とユーザーの予想のみ | 上限を仮定して設計しない |

既存8シードは今回の結果を知った時点で、次の方策にとっては既知データ。
`split=heldout` はG6D01当時の分割名であり、次の実験にも未使用であることを意味しない。

## 3. P0: 判断できる評価集合を作る

目的は評価基盤の刷新ではなく、現行版にとって難しい相手の追加。

- 最新の現行提出について、可能なら直近20〜30件を時系列で取得し、勝ちも負けも残す。目標件数は収集予算であり、統計的十分性の保証ではない。
- submission ID、日時、相手、席、シード、店舗、勝敗を保存。重複episodeを除く。
- 掘り下げ用に敗戦を多く選んだ集合と、勝率推定用の時系列集合を分ける。
- farm構成・拡張時刻・実現生産・売却挙動で相手を分類する。作者名・版番号だけで独立familyと数えない。
- 公開コードがある強い相手はexact source/hash/entrypointを固定。Tetsuが引き分け・敗戦になる非同型を優先する。
- ソースのない相手のリプレイは診断専用。固定行動列や粗い模倣を、本人との再判断あり対戦と呼ばない。
- 今回の3公開相手は回帰確認用に残すが、これだけの全勝を根拠にしない。

取得が使える接続先の制約で止まったら、既存データで機序検証を続け、対戦母集団が不足と明記する。制限回避を試みない。今回のブラウザー取得は非HTTP URLの制約で中断した。

## 4. P1: 卵の回収と肥料確保を対にする

### 4.1 最小の実装範囲

現行Tetsuのopening、既存経路、動物数、作物構成、販売タイミングは基準として保つ。
判断対象は、成熟・給餌済みのガチョウで `COLLECT_FERTILIZER` と `HARVEST` が競合する場面。
まず次の3案を比較する。

1. 基準行動をそのまま実行。
2. 卵を回収し、失う肥料を既存在庫の余剰で賄う。
3. 卵を回収し、必要分の肥料を使用期限までに市場で補う。

移動や追加雇用を最初から探索しない。資源収支の比較が成立してから広げる。
単独回収のG6D01は比較対照として凍結し、閾値をいじって名前を保たない。

### 4.2 比較する価値

概念式:

`回収で防げる生産損失の換金価値 − 肥料を賄う費用 − 追加廃棄/作業/資金不足による損失`

- 早く回収した卵全量を追加生産と数えない。基準版が後で回収する分を差し引く。
- 肥料の価値を売値だけにしない。予定した施肥で増える作物、補充可能性、資金・注文枠の制約を含める。
- 補充可能なら買い戻し費用で下流の減産を避ける。補充不可能なら失う作物の価値を計上する。
- 同じ不足について「補充費用」と「補充後には発生しない減産」を二重計上しない。
- 持ったまま終わる商品を額面価格で利益認定しない。実行可能な回収・搬入・売却まで見る。

### 4.3 予測と資源の約束

最初は当日〜翌日の48ターン以内を中心に、既定経路の観測可能な予定を読む。3日間の施肥効果など期間外に残る影響には保守的な残存価値を置き、そこが採否を左右する場合だけ期間を延ばす。
これは近似。未来のショップ・相手行動・相手の私有在庫は見ない。

追跡するもの:

- 当日中の既定HARVEST予定と、次の回収までの動物の保持上限。
- 自分のshedと各workerの荷物。肥料の所在、予定PICKUP、施肥先・期限。
- 実際に買えた補充分、残る不足量、余った補充分。購入要求数を入庫数として扱わない。
- 同turnはunit→market順。使用直前の購入では間に合わない場合がある。
- 倉庫容量、日末の自動移送・廃棄、投入資金、最大注文数。
- 親の余剰肥料売却層と予約量を共有し、補充直後に売ってしまう矛盾を避ける。

予約の実装は品目・必要量・期限の小さなledgerから始める。
既存の投入計画と同じ肥料を二重に予約しない。取引失敗やworker数の変化で予約を見直す。

### 4.4 実装で壊しやすい点

- 親の `agent` を実turnに2回呼ばない。内部状態が進んでしまう。
- 仮想比較は分離したfarm/private/計画状態で行い、採用前に本体の予約・統計を書き換えない。
- `_g6_apply` は現在、最終market処理後にunit actionを変える。新方策では変更後の棚在庫・資金・注文を再検証する。親が作った古い在庫予測をそのまま信じない。
- mutableなmodule全体を無理にdeepcopyする全面fork機構は作らない。必要な局所状態だけを明示的に渡す。
- 予測誤差で投入作業を壊す場合は基準行動に戻す。fallback理由と件数を記録し、例外握りつぶしで成功扱いにしない。
- 後日・別シードでしか起きない条件を、episode IDや固定stepの例外で修正しない。

### 4.5 最初の比較と完了条件

同じ条件で「現行Tetsu / 凍結G6D01 / 新しい資源連動版」を比較する。
旧8シードは不具合確認と開発に使える。新方策の最終確認は別シード。
記録する値:

- 回収変更回数、予測した損失回避量、実現した卵の収穫/売却増。
- 肥料回収減、追加購入量/費用、実行できた施肥、作物別の実現収穫差。
- 日末廃棄、販売完了量、残在庫、資金不足・予定投入失敗。
- W/D/L、引き分けを0.5とした得点、相手別/店舗別/席別の差。

成功は「卵が増えた」ではなく、資源不足による意図しない減産を抑え、難しい非同型相手を含む対戦でも利益と勝敗の改善が見えること。
予測が外れた具体的な遷移を説明できなければ、パラメータ調整前にその不一致を直す。

## 5. P2: 終盤の作付けを作業束で選ぶ

P1で一つ結論を出した後に進む。P1が採用ならその版、保留/棄却なら現行Tetsuを基準にする。
最初の範囲は内部step648以降。既存ルート2を全面破棄しない。

比較対象は基準、実行可能な少数のニンジン転換、作付けせず回収に労働を回す案。
種の購入・同時PLANT制約・成熟・水やり・肥料・移動・回収・倉庫・最終売却を一つのplanとして評価する。
最後まで収穫できない作物は利益0として扱う。生育済み作物を掘り返す費用も含める。
既知の店舗から需要を計算し、相手供給の低/中/高シナリオで追加数量の限界売上を比べる。
小麦は販売品と飼料の両方なので、餌が不足する転換は除く。

Ace戦の作付けを固定コピーしない。異なる店舗構成でも成立するかを検証し、P1とP2を単独/組合せで分けて測る。

## 6. P3: 販売を生産予測へ接続する

既存Tetsuにも相手を意識した前倒し・注文並べ替えがある。「相手を考慮すれば新規改善」とは扱わない。
最初は余剰の現金化商品を対象に、短い将来区間で今売る量/待つ量/順番を比較する。
飼料・肥料・種・土地・雇用への支払いを崩さない。

- 相手供給は、供給なし、同系統の同時売り、前後にずれたまとまった供給など複数仮定。
- 公開盤面の成熟量や市場変化で仮定を更新できるが、隠れた在庫の正解を復元できたと主張しない。
- 市場注文の同時引用、注文index、需要消費、価格1の処理を公式に合わせる。
- 期間末の在庫価値は、続く搬入・容量・売却予定込みで保守的に評価。
- この段階で全ゲームMCTS/RLや大量の定数総当たりに広げない。

`timing_diagnostic.py` は未来の相手行動を参照するため、そのままagentへ移植してはいけない。

## 7. 評価と昇格の具体的手順

以下の件数は作業予算の初期設定であり、統計的保証ではない。

1. Integrity: 少数の両席ゲームで例外、DONE、資源収支、入口関数、実行時間を確認。
2. Development: 既知8シードを含む小集合で機序と副作用を説明。ここで仕様を固める。
3. Confirmation: コードhashと相手一覧を先に固定し、未使用16シード程度・両席で基準と候補を対比較。少なくとも苦戦する非同型相手を含める。
4. 各相手の平均得点差を同じ重みで集計する。母集団頻度で重み付けするなら、勝敗を見てから重みを変えず、根拠を記録。
5. 同一seed・両席・複数相手の相関を保ってseed単位で集計/再標本化する。単純に対戦数を独立標本数としない。
6. 勝敗改善が非同型にもあり、特定の主要な相手/店舗での再現性ある悪化を説明・許容でき、実行制約を満たす場合に昇格候補とする。
7. 不確実なら「保留」。コードを変えず別seedで一度だけ追加確認する。確認結果を見て変更したら、その確認集合は開発データへ移す。

「16seedで有意差が必ず出る」「marginが正なら提出」という閾値は設けない。
逆に、サンプル不足を理由に際限なく同じ弱いパネルを回すこともしない。
live比較では提出からの経過、対戦数、相手、同時期の基準を残し、短時間のrating上昇だけで因果判断しない。

## 8. この枝を止める条件

- P1は、資源連動版と、失敗理由が説明できる一度の設計修正までを最初の予算とする。単なる実装バグ修正は別。
- それでも改善が同型限定、重要な作物収穫を繰り返し壊す、または動作を説明できないなら凍結しP2へ進む。
- P2も新しい店舗条件で再現しなければ、固定の作物枚数調整で粘らず棄却。
- 異なる生産構成の強い公開候補が見つかり、fresh comparisonで明確に有望なら、そちらを主線候補へ変更してよい。
- 生産損失が主因でない新しいライブ証拠が増えたら、P3を先にする。変更理由をcurrent stateへ残す。
- M-familyの再開は、安定した実行可能な生産束の仮説があり、現行線より期待値が高いと判断できる場合に限る。E33の続きという理由では再開しない。

期限が迫ったら未検証の全面変更を止め、証拠のある提出線を維持する。残日数・枠・規則は実画面で再確認する。

## 9. 終了時に必ず残すもの

- 新しい実験ID、基準/候補/相手/公式環境のhash、入口関数、コードとコマンド。
- 何を変更し何を比較固定したか、開発/確認seed一覧、全ゲーム結果。
- 仮説、実現した機序、副作用、W/D/Lと不確実性、採用/保留/棄却の理由。
- 次の一手と中止条件。未実行事項は未実行と書く。
- README・current state・実験索引・実行履歴を同じ現在地にそろえる。

今回の持ち帰りは「卵8/12個の小さな修正を積み上げる」ことではない。
**利益が失われる場所を実測し、他の資源を犠牲にせず直し、難しい対戦相手への勝敗で判断する開発手順を固定すること**である。
