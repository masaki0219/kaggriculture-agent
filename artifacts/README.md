# Artifacts

- `bundles/current/` is the canonical active elite bundle formerly named
  `kaggriculture_elite_bundle_PATCHED_v2/`.
- `bundles/current_snapshot.zip` is the original byte-preserved
  `kaggriculture_elite_bundle_PATCHED_v2.zip`; it was not regenerated.
- `submissions/` contains byte-preserved submission archives.

Historical bundles and intermediate packaging attempts are under `../archive/`.
