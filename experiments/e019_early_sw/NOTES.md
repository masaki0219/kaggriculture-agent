# E19 — early SW prebuild

- Status: unresolved mechanism experiment; not promoted.
- The recorded direct run activated early land but did not activate planting or
  watering, so the repository does not relabel the underlying hypothesis.
- `agent.py`, `arena.py`, `cache.json`, and `results.json` are canonical.
- Compatibility symlinks preserve the original import and output names without
  editing the historical agent.
