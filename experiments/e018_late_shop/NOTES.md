# E18 — late-shop tape switch

- Status: historical, rejected by the recorded experiment.
- `agent.py`, `arena.py`, `cache.json`, and `results.json` are the canonical
  experiment files.
- Compatibility symlinks preserve the original module and result/cache names;
  they prevent path-only reorganization from changing the agent source.
- The agent and recorded cache/results were moved byte-for-byte.
